AppBundle\Entity\Examen:
    type: entity
    table: null
    repositoryClass: AppBundle\Repository\ExamenRepository
    id:
        id:
            type: integer
            id: true
            generator:
                strategy: AUTO
    fields:
        date:
            type: date
        note:
            type: float
            nullable: true
        rendu:
            type: boolean
    lifecycleCallbacks: {  }
    oneToMany:
      reponses:
        cascade: ["remove", "persist"]
        targetEntity: Reponse
        mappedBy: examen
    manyToOne:
      apprenant:
        targetEntity: Utilisateur
        inversedBy: examens_apprenant
        joinColumn:
          name: apprenant_id
          referencedColumnName: id
      correcteur:
        targetEntity: Utilisateur
        inversedBy: examens_correcteur
        joinColumn:
          name: correcteur_id
          referencedColumnName: id
      questionnaire:
        targetEntity: Questionnaire
        inversedBy: examens_questionnaire
        joinColumn:
          name: questionnaire_id
          referencedColumnName: id
